input LoginInput {
  username: String!
  password: String!
}

type AuthPayload {
  user: User!
}

extend type Query {
  me: User
}

extend type Mutation {
  login(input: LoginInput!): AuthPayload!
  logout: Boolean!
  refreshToken: AuthPayload!
}
