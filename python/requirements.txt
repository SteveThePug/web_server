fastapi==0.115.6
uvicorn[standard]==0.34.0
requests==2.32.3

# Dev only, deliberately commented out so the image stays slim: the test suite
# (python/tests) needs pytest. Install it with `pip install pytest` locally and
# run `python -m pytest` from this directory.
# pytest==9.0.3
